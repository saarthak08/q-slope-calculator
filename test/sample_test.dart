import 'package:flutter_test/flutter_test.dart';

void main() {
  group("Group: getter obj type tests", () {
    bool working = true;
    test("Test: check tests working fine", () {
      expect(working, true);
    });
  });
}
